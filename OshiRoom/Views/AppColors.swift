import SwiftUI

/// アプリ全体で使う、読みやすさを優先した色です。
enum AppColors {
    static let textPrimary = Color(red: 0.07, green: 0.08, blue: 0.10)
    static let textSecondary = Color(red: 0.24, green: 0.26, blue: 0.30)
    static let textMuted = Color(red: 0.36, green: 0.38, blue: 0.42)
    static let separator = Color.black.opacity(0.12)
    static let surface = Color.white.opacity(0.94)
    static let elevatedSurface = Color.white.opacity(0.98)
}
